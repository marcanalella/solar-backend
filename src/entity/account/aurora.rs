/*use uuid::Uuid; // Import Uuid
use crate::account::Account;

#[derive(Debug, Clone)]
pub struct Account {
    pub account: Account,
    pub aurora_vision_apikey: String
}

// Implement methods for AuroraEntity
impl Account {
    pub fn new(account: Account, username: &str, password: &str, aurora_vision_apikey: &str) -> Self {
        Account {
            account: Account::new(user, password),
            aurora_vision_apikey
        }
    }
}*/